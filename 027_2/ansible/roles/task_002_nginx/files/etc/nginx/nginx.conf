events {
    worker_connections 1024;
}

http {
    server {
        listen       80 default_server;
        server_name  default_server;
        root         /usr/share/nginx/html;

        location / {
            autoindex on;
        }
    }
}